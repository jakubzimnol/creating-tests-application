This backends apps for creating tests.
To run it set virtual environment:
python3 -m venv myvenv

Then upgrade pip :
python3 -m pip install --upgrade pip

Next move to backend folder and install all requirements:
cd backend
pip install -r requirements.txt

After that start server by:
./manage.py runserver
